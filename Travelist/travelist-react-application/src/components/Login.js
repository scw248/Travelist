import React from 'react'

const Login = () => {
  return (
    <form onSubmit={}>
      <input type="text" name="email" onChange={} value={}/>
      <input type="text" name="password" onChange={} value={}/>
    </form>
  )
}

export default Login